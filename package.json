{
  "name": "am-kelavi-benefits",
  "version": "2.0.0",
  "description": "Benefits platform - Vercel optimized",
  "scripts": {
    "dev": "vercel dev --port 3000",
    "start": "vercel dev --port 3000",
    "serve": "npx http-server . -p 3000 -o",
    "build": "echo 'No build step required for static site'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": "18.x"
  }
}